export const RoutesPath = Object.freeze({
    dashboard: '/',
    auth: Object.freeze({
        login: '/login',
    }),
    test: Object.freeze({
        test: 'test',
    }),
    user: Object.freeze({
        articles: 'articles',
    }),
    error: Object.freeze({
        notFound: '/404',
    }),
})
